// Make the waypoints module and its contents public
pub mod waypoints;

// Make the core (or main) cavebot logic module public
pub mod core;

// Make the tasks module and its contents public
pub mod tasks;

// Re-export key structs for easier access if desired
// pub use waypoints::{Waypoint, WaypointType};
// pub use core::Cavebot;
// pub use tasks::CavebotTask;
